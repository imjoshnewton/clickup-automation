# ============================================
# SHARED CONFIGURATION
# ============================================

# Server Configuration
PORT=8001                              # Port for webhook servers (both platforms)

# Paths
REPO_PATH=/path/to/your/repository     # Target repository for automation
CLAUDE_CODE_PATH=claude                # Path to claude binary (or just 'claude')
LOG_DIR=./logs
WORKTREE_BASE_DIR=/tmp/claude-automation  # Git worktree base directory

# API Keys
ANTHROPIC_API_KEY=your-anthropic-api-key  # For Claude Code SDK

# ============================================
# GITHUB CONFIGURATION (EXISTING)
# ============================================

# GitHub Authentication (Optional)
# If not set, uses 'gh auth login' credentials
GITHUB_PAT=

# GitHub-specific settings
E2B_API_KEY=                           # Agent Cloud Sandbox (optional)
CLOUDFLARED_TUNNEL_TOKEN=              # For exposing webhook (optional)
CLAUDE_BASH_MAINTAIN_PROJECT_WORKING_DIR=true

# ============================================
# CLICKUP CONFIGURATION (NEW)
# ============================================

# ClickUp Authentication (Required for ClickUp)
CLICKUP_API_KEY=your-clickup-api-key
CLICKUP_WEBHOOK_SECRET=your-webhook-secret-here

# ClickUp-specific settings
CLICKUP_LIST_ID=                       # Optional: filter tasks to this list
CLICKUP_TEST_TASK_ID=                  # Optional: for health check testing
CLICKUP_WEBHOOK_PATH=/webhook/clickup  # Webhook endpoint path (default: /webhook/clickup for compatibility with server.ts)

# ============================================
# OPTIONAL: SSL CONFIGURATION
# ============================================

SSL_CERT_PATH=/path/to/cert.pem
SSL_KEY_PATH=/path/to/key.pem

# ============================================
# LEGACY/DEPRECATED (from old server.ts)
# ============================================

# IP Whitelisting (Optional - ClickUp doesn't provide fixed IPs)
# ALLOWED_IPS=